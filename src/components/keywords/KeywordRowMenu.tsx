"use client";

import React from "react";

/** Status changes use the row `<select>`; the kebab menu is retired. */
export const KeywordRowMenu = React.memo(function KeywordRowMenu() {
  return null;
});
